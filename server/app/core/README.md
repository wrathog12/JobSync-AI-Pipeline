# /server/app/core/

Application configuration, settings, and constants.

## Planned modules

| File | Responsibility |
|------|---------------|
| `config.py` | Environment-based settings (model provider keys, DB URIs, token ceilings) |
| `constants.py` | Fixed values — competency taxonomy enum, field classification categories |
| `exceptions.py` | Custom exception hierarchy for structured error responses |
