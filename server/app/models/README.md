# /server/app/models/

Database / ORM models (if using a persistent store server-side).

> For the local-first architecture (recommended), the server is mostly stateless.
> Models here are for: user accounts, adapter versions, telemetry aggregates, field classification cache.
