# /extension/pages/offscreen/

HTML page for the MV3 offscreen document.

- `offscreen.html` — Hidden document hosting compute tasks (dense embedding inference via transformers.js WASM). Not visible to user. Phase 3.
